PORT=3210
HOST=0.0.0.0
REMINDER_INTERVAL_MS=600000

# 邮件通知（必填后才可真实发送）
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=demo@example.com
SMTP_PASS=your-password
SMTP_FROM=demo@example.com
# 页面里可单独维护“发件人名称 / 发件人邮箱”；这里主要是 SMTP 连接账号

# 短信通知（演示版为 mock 日志写入）
SMS_ENABLED=false
